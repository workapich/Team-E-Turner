window.addEventListener("DOMContentLoaded", vlada);



function vlada() {
    let cart = [];
    let j = 0;
    if (cart.length == 0) {
        const lista = document.getElementById("cartItem");
        lista.innerHTML = "prazna korpa";

    }
}